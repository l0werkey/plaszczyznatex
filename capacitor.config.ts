import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'me.szymon.plaszczyznatex',
  appName: 'Plaszczyznatex',
  webDir: 'build'
};

export default config;
